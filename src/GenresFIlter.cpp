#pragma once

#include <vector>
#include <iostream>
#include "../Bibliotecas/GenresFilter.hpp"

void getGenresFilter(const ArrayList<Filme> &filmes, ArrayList<int> &genresFilter, Genres genres, ArrayList<int> &lista)
{
    for (int i = 0; i < filmes.getSize(); i++)
    {
        if (filmes[i].genres != genres)
        {
            genresFilter[i].isHere = false;
        }
        genresFilter.isHere = true;
        vector<int> j = j.add(i);
    }
    for (int i = 0; i < genresFilter.getSize(); i++)
    {
        if (genresFilter.isHere)
            lista[i] =
    }
}
//Testando o pc de Samuelsds
dsasdlpsaadsad
ddsf